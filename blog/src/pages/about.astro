---
import BaseLayout from '../layouts/BaseLayout.astro'
---

<BaseLayout title="关于" description="CodeAtlas 产品边界、企业部署与技术架构说明。">
  <article class="about-page">
    <p class="eyebrow">PRODUCT AND ARCHITECTURE</p>
    <h1>关于 CodeAtlas</h1>
    <p class="lead">CodeAtlas 是面向企业研发团队的版本化私有代码知识库与 MCP 检索服务。系统通过仓库授权、角色权限和 Token 范围隔离访问，并为检索结果保留可验证的代码证据。</p>
    <section>
      <h2>产品边界</h2>
      <p>系统负责 Git 同步、代码解析、秘密脱敏、混合检索、权限控制和可审计的 MCP 工具调用。部署架构以清晰的数据边界、可预测的资源使用和可恢复的索引流程为核心。</p>
    </section>
    <section>
      <h2>技术选择</h2>
      <p>FastAPI 承载 REST 与 Streamable HTTP，MySQL 8 保存业务状态并通过 FULLTEXT 提供词法检索，Chroma 保存向量。Java、Python、JavaScript 与 TypeScript 使用 Tree-sitter，其他文本格式按限长窗口切片。</p>
    </section>
    <section>
      <h2>访问边界</h2>
      <p>受控公开实例仅使用许可证宽松的开源仓库，用于产品评估与接口验证。企业私有部署的数据、凭据和向量索引保持在独立环境内，不会进入公开实例。</p>
    </section>
  </article>
</BaseLayout>

<style>
  .about-page { width: min(760px, calc(100% - 48px)); margin: 0 auto; padding: 88px 0 110px; }
  .eyebrow { margin: 0; color: var(--accent); font-size: 11px; font-weight: 760; }
  h1 { margin: 10px 0 22px; font-size: 42px; }
  .lead { color: #3f4a50; font-size: 18px; line-height: 1.8; }
  section { margin-top: 52px; padding-top: 22px; border-top: 1px solid var(--border); }
  h2 { margin: 0 0 14px; font-size: 20px; }
  section p { margin: 0; color: var(--muted); font-size: 14px; line-height: 1.9; }
  @media (max-width: 640px) { .about-page { width: calc(100% - 28px); padding: 58px 0 80px; } h1 { font-size: 34px; } }
</style>
